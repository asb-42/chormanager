# Plan: M-4 — Findings der Code-Review 2026-06-14 (Hauptdatei)

**Datum:** 2026-06-14
**Bezug:** `docs/reports/2026-06-14_code-review.md`
**Methodik:** Feinteilige Todo-Liste, die alle 35 Findings (C-1 bis C-6, M-1 bis M-8, m-1 bis m-10, S-1 bis S-3, CC-1 bis CC-5, R-1 bis R-6, A-1 bis A-6, T-1 bis T-5) systematisch abarbeitet.
**Markierung:** 🔍 **SUB-PLAN ERFORDERLICH** = Aufgabe braucht eigene Detail-Plan-Datei, weil Umfang > 1 Sprint oder Architektur-Entscheidung nötig.

> **Konvention:** Jede Aufgabe hat eine eindeutige ID (z.B. C2-FIX-A), Aufwand-Schätzung (XS / S / M / L / XL), Priorität (P0 = vor Release, P1 = vor nächstem Feature, P2 = nice-to-have), Abhängigkeiten, und Akzeptanzkriterien.

---

## 🎯 Übersicht

| Prio | Anzahl | Aufwand |
|---|---:|---:|
| **P0** (Must-Fix vor Release) | 11 | ~9,5 Tage |
| **P1** (Should-Fix vor nächstem Feature) | 14 | ~13,5 Tage |
| **P2** (Nice-to-have) | 19 | ~12 Tage |
| **Σ** | **44** | **~35 Personentage** |

🔍 **5 Sub-Pläne erforderlich** (C-1, C-3, C-4, C-6, A-1) — siehe Anhang B.

**Aufteilung der Plan-Dateien:**
- `2026-06-14_m4_findings.md` (diese Datei): Übersicht, P0-Cluster, Sprint-Planung
- `2026-06-14_m4_findings_phase2_p1.md`: P1-Cluster
- `2026-06-14_m4_findings_phase3_p2.md`: P2-Cluster
- `2026-06-14_m4_anhang_b_subplans.md`: Sub-Plan-Übersicht

---

## 🔴 Phase 1 — Must-Fix vor Release (P0)

### Cluster A: Datenintegrität

#### C2-FIX-A · `OptimizeFormationCommand` Doppel-Redo auflösen
- **Prio:** P0 · **Aufwand:** S (0,5 Tag) · **Abhängigkeiten:** —
- **Akzeptanz:** (1) `FormationOptimizer.run()` baut `cmd` ohne initialen `redo()`. (2) `grid.undo_stack.push(cmd)` ruft `cmd.redo()` genau einmal auf. (3) `swap_count` korrekt. (4) `old_positions` und `new_positions` konsistent. (5) Test T-1 grün.
- **Subtasks:**
  - [ ] **C2-FIX-A.1** Test `tests/unit/test_optimizer_double_redo.py`: Mock-Singer, Optimizer `[height, affinity]`, `swap_count` vor/nach `push()` assertieren.
  - [ ] **C2-FIX-A.2** In `chormanager/choraufstellung/core/optimizer.py:88-89` `cmd.redo()` entfernen. `cmd = OptimizeFormationCommand(...)` (Constructor captured `old_positions`), dann `grid.undo_stack.push(cmd)`.
  - [ ] **C2-FIX-A.3** `cmd.new_positions` in `redo()` (Z. 45-46) bleibt unverändert.
  - [ ] **C2-FIX-A.4** Audit: andere `grid.undo_stack.push(...)`-Caller in `widgets/formation_grid.py` (Z. 285, 644, 675) prüfen.
  - [ ] **C2-FIX-A.5** Test-Suite grün.

#### C5-FIX-A · Auto-Save: Symlink durch atomic file replace ersetzen
- **Prio:** P0 · **Aufwand:** S (0,5 Tag) · **Abhängigkeiten:** —
- **Akzeptanz:** (1) `FormationStorage.save_autosave` schreibt `latest_autosave.json` direkt (kein Symlink). (2) Atomic via `tmp + os.replace`. (3) `get_latest_autosave_path` returnt neuesten timestamped File. (4) Windows-kompatibel. (5) Test T-5 grün.
- **Subtasks:**
  - [ ] **C5-FIX-A.1** Refactor `chormanager/choraufstellung/storage.py:129-153`: `latest_autosave.json` als normaler File via `tmp + os.replace`. Symlink entfernen.
  - [ ] **C5-FIX-A.2** `get_latest_autosave_path` (Z. 172-184) anpassen: `os.listdir(backup_dir)` filtern, nach `os.path.getmtime` sortieren.
  - [ ] **C5-FIX-A.3** `delete_latest_autosave` (Z. 193-207) anpassen.
  - [ ] **C5-FIX-A.4** Test T-5 `tests/unit/test_storage_autosave_windows.py`: prüft, dass kein `os.symlink` aufgerufen wird.
  - [ ] **C5-FIX-A.5** `_get_backup_dir` (Z. 121-127) auf XDG-Pfade: `appdirs.user_data_dir("choraufstellung")`. **Blocker für pip-install.**
  - [ ] **C5-FIX-A.6** Test grün.

#### S1-FIX-A · SQL-Whitelist in `domain/repository.py`
- **Prio:** P0 · **Aufwand:** S (0,5 Tag) · **Abhängigkeiten:** —
- **Akzeptanz:** (1) `kwargs`-Keys gegen `_SINGER_COLS` / `_EVENT_COLS` / `_BESETZUNG_COLS` / `_REPERTOIRE_COLS` whitelist-geprüft. (2) Unbekannte Keys werfen `ValueError`. (3) Bestehende Tests grün. (4) Bandit-CI.
- **Subtasks:**
  - [ ] **S1-FIX-A.1** Helper `_validate_columns(kwargs, allowed: set)` in `chormanager/domain/repository.py`.
  - [ ] **S1-FIX-A.2** In `SingerRepository.create/update`, `EventRepository.create/update`, `ProjectRepository.create/update`, `BesetzungRepository.create/update`, `RepertoireRepository.create/update`: vor `f"INSERT/UPDATE ..."` `_validate_columns(kwargs, ...)` aufrufen.
  - [ ] **S1-FIX-A.3** `is_adult` whitelisten (Klärung: Property oder persistiert?).
  - [ ] **S1-FIX-A.4** Test `tests/unit/test_repository_sql_safety.py`: parametrische Tests.
  - [ ] **S1-FIX-A.5** `bandit -r chormanager/domain/ --severity-level high` in CI.
  - [ ] **S1-FIX-A.6** Test grün.

#### M4-FIX-A · `state.json` atomic write
- **Prio:** P0 · **Aufwand:** XS (0,1 Tag) · **Abhängigkeiten:** —
- **Akzeptanz:** (1) `save_state` schreibt `state.json.tmp + os.replace`. (2) Bei Crash bleibt `state.json` intakt.
- **Subtasks:**
  - [ ] **M4-FIX-A.1** `chormanager/config.py:33-41` auf atomic-write-Pattern.
  - [ ] **M4-FIX-A.2** Test `tests/unit/test_config_state_atomic.py`.
  - [ ] **M4-FIX-A.3** Test grün.

### Cluster B: Update-Sicherheit (C-3)

#### 🔍 C3-SUBPLAN-A · Eigenes Sub-Plan: `update_controller._do_update` Timeout + Signaturprüfung
- **Prio:** P0 · **Aufwand:** M (1 Tag) — siehe Sub-Plan
- **Sub-Plan-Datei:** `plans/2026-06-14_subplan_update_controller.md` (zu erstellen)
- **Warum Sub-Plan?** Drei Sub-Bereiche: (a) QThread-Migration, (b) Timeout-Ergänzung, (c) `git verify-commit`. Sicherheits-Implikation (RCE via kompromittiertes Remote).
- **Akzeptanz:** (1) `_check_version` läuft in QThread. (2) `_do_update` hat `timeout=60`. (3) `git verify-commit HEAD` nach Pull. (4) Test T-4 grün. (5) Kein UI-Freeze.
- **Subtasks:**
  - [ ] **C3-SUBPLAN-A.0** Sub-Plan-Datei erstellen.
  - [ ] **C3-SUBPLAN-A.1** `QApplication.processEvents()` (Z. 57, 99) durch `QApplication.setOverrideCursor(Qt.WaitCursor)` ersetzen.
  - [ ] **C3-SUBPLAN-A.2** `QThread`-Worker `UpdateCheckWorker` mit `pyqtSignal(dict)`.
  - [ ] **C3-SUBPLAN-A.3** `UpdateWorker` für `git pull` mit `timeout=60`.
  - [ ] **C3-SUBPLAN-A.4** `git verify-commit HEAD` nach erfolgreichem Pull.
  - [ ] **C3-SUBPLAN-A.5** Auto-Update erzwingt App-Neustart.
  - [ ] **C3-SUBPLAN-A.6** Test T-4 `tests/unit/test_update_controller.py` mit gemocktem `subprocess.run`.
  - [ ] **C3-SUBPLAN-A.7** Test grün + manueller Test mit GitHub.

### Cluster C: Recovery & Concurrency

#### M6-FIX-A · AutoSaveController während Recovery pausieren
- **Prio:** P0 · **Aufwand:** XS (0,2 Tag) · **Abhängigkeiten:** —
- **Akzeptanz:** (1) `RecoveryController.check()` ruft `self._host.autosave.stop()` vor `load_formation()`. (2) `self._host.autosave.start()` nach Restore. (3) Test T-3 grün.
- **Subtasks:**
  - [ ] **M6-FIX-A.1** In `chormanager/choraufstellung/recovery.py:68` am Anfang: `self._host.autosave.stop()`.
  - [ ] **M6-FIX-A.2** `try/except` um `self._host._load_formation_data(data)`: bei Erfolg `self._host.autosave.start()`, bei Fehler ebenfalls.
  - [ ] **M6-FIX-A.3** Test T-3 `tests/unit/test_recovery_race.py` mit `QTimer.singleShot(0, autosave_controller.check)` während Recovery-Load.
  - [ ] **M6-FIX-A.4** Test grün.

#### m1-FIX-A · `RecoveryController` setzt `host.file = None` nach Restore
- **Prio:** P0 · **Aufwand:** XS (0,1 Tag) · **Abhängigkeiten:** —
- **Akzeptanz:** (1) Nach erfolgreichem Restore triggert `save_f()` ein Save-As.
- **Subtasks:**
  - [ ] **m1-FIX-A.1** In `chormanager/choraufstellung/recovery.py:95` `self._host.file = None` setzen (statt `latest`).

#### R3-FIX-A · `OptimizeFormationCommand.undo` mit `tiles.clear()`
- **Prio:** P0 · **Aufwand:** XS (0,1 Tag) · **Abhängigkeiten:** C2-FIX-A
- **Akzeptanz:** (1) Vor `refresh_grid()` in `undo()` wird `self.grid.tiles.clear()` aufgerufen.
- **Subtasks:**
  - [ ] **R3-FIX-A.1** In `chormanager/choraufstellung/core/optimizer.py:50-54` `self.grid.tiles.clear()` einfügen, falls `hasattr(self.grid, 'tiles')`.
  - [ ] **R3-FIX-A.2** Test `test_optimizer_undo_stale_tile.py`.

### Cluster D: Zuverlässigkeit

#### R1-FIX-A · `AffinityRule` Bounds-Check via `GridEngine.is_valid_position`
- **Prio:** P0 · **Aufwand:** S (0,3 Tag) · **Abhängigkeiten:** —
- **Akzeptanz:** (1) Vor jedem Swap in `apply()`: `GridEngine.can_place(nr, nc, placed)` als Gate. (2) Out-of-bounds-Swaps werden verworfen.
- **Subtasks:**
  - [ ] **R1-FIX-A.1** `GridEngine`-Instanz in `AffinityRule.apply` injecten.
  - [ ] **R1-FIX-A.2** In `chormanager/choraufstellung/core/rules.py:394-396` Bounds-Check: `if not engine.is_valid_position(nr1, nc1) or not engine.is_valid_position(nr2, nc2): continue`.
  - [ ] **R1-FIX-A.3** Test `test_affinity_out_of_bounds_swap.py` mit `rows=2, cols=2`, 10 Sängern.

#### R2-FIX-A · `VoiceGroupCohesionRule` Sanity-Check
- **Prio:** P0 · **Aufwand:** XS (0,1 Tag) · **Abhängigkeiten:** —
- **Akzeptanz:** (1) `assert s1.singer_id != s2.singer_id` und `s1.singer_id != other.singer_id` in `chormanager/choraufstellung/core/rules.py:584, 615`.
- **Subtasks:**
  - [ ] **R2-FIX-A.1** Sanity-Asserts einfügen.
  - [ ] **R2-FIX-A.2** Test `test_cohesion_sanity_check.py`.

#### R4-FIX-A · `formation_grid.apply_affinity_proximity` Bounds-Check
- **Prio:** P0 · **Aufwand:** XS (0,1 Tag) · **Abhängigkeiten:** —
- **Akzeptanz:** (1) Bounds-Check vor `partner.row, partner.col = ...` (Z. 313). (2) `target_col` out-of-bounds → return False.
- **Subtasks:**
  - [ ] **R4-FIX-A.1** In `chormanager/choraufstellung/widgets/formation_grid.py:309-316` `if not (0 <= target_col < self.cols and 0 <= singer.row < self.rows): return False`.
  - [ ] **R4-FIX-A.2** Test schreiben.

#### R5-FIX-A · `save_formation` `tmp`-Cleanup bei Crash
- **Prio:** P0 · **Aufwand:** XS (0,1 Tag) · **Abhängigkeiten:** —
- **Akzeptanz:** (1) Bei Exception zwischen `open(tmp, 'w')` und `os.replace(tmp, target)` wird `os.remove(tmp)` aufgerufen.
- **Subtasks:**
  - [ ] **R5-FIX-A.1** In `chormanager/choraufstellung/storage.py:62-65` `try/except` mit `finally`-Cleanup.
  - [ ] **R5-FIX-A.2** Test schreiben.

---

## 📅 Vorgeschlagene Sprint-Reihenfolge (P0 + P1)

### Sprint 1 (3-4 Tage) — P0 Cluster A + B + C
1. **C5-FIX-A** (Symlink, 0,5 d) — schneller Win
2. **C2-FIX-A** (Optimizer-Doppel-Redo, 0,5 d)
3. **M4-FIX-A** (state.json atomic, 0,1 d)
4. **M6-FIX-A, m1-FIX-A, R3-FIX-A** (Recovery + Undo, 0,5 d)
5. **R1-FIX-A, R2-FIX-A, R4-FIX-A, R5-FIX-A** (Bounds-Checks, 0,6 d)
6. **S1-FIX-A** (SQL-Whitelist, 0,5 d)
7. **C3-SUBPLAN-A** (Update-Controller, 1 d)

### Sprint 2 (3-4 Tage) — P1 Cluster E + F + G
1. **C1-SUBPLAN-A** (Subshell-IPC, 3-5 d, in 2 Sprints)
2. **C4-SUBPLAN-A** (Optimizer-Perf, 2 d)
3. **C6-SUBPLAN-A** (DB-Connection-Pool, 2 d)
4. **M1-FIX-A** (Subprocess-Runner, 1,5 d)
5. **M5-FIX-A, A5-FIX-A** (load_formation_data, 0,6 d)
6. **T1 bis T5** (Tests, parallel, 1,3 d)

### Sprint 3 (2-3 Tage) — P1 Cluster H + P2 Quick-Wins
1. **M2-FIX-A, m8-FIX-A, m9-FIX-A** (Error-Handling, 0,8 d)
2. **R6-FIX-A, m6-FIX-A, m7-FIX-A** (Repository-Cleanup, 0,6 d)
3. **A1-SUBPLAN-A** (Mixin-Refactor, 2-3 d)
4. **A2-FIX-A, A3-FIX-A, DEADCODE-FIX-A** (Cleanup, 1 d)

### Sprint 4 (optional) — P2 Polish
1. **CC3-FIX-A, CC4-FIX-A, m3-FIX-A, m5-FIX-A** (UI-Polish, 2 d)
2. **HYPOTHESIS-FIX-A, MUTMUT-FIX-A** (Test-Strength, 2 d)
3. **m10-FIX-A, S2-FIX-A** (Doku/Sortierung, 0,2 d)

---

## 📊 Aufwand-Schätzung Gesamt

| Phase | Cluster | Aufwand |
|---|---|---:|
| **P0** | A, B, C, D | **~9,5 Tage** |
| **P1** | E, F, G, H, I | **~13,5 Tage** |
| **P2** | J, K, L, M, N, O | **~12 Tage** |
| **Σ** | | **~35 Personentage** |

> **Realistische Schätzung mit Overhead** (Reviews, Refactor, Re-Test): **~50 Personentage = ~10 Wochen** für P0 + P1, **~16 Wochen** für alles.

---

## 🏁 Akzeptanz-Gates pro Sprint

- [ ] **Sprint 1:** `pytest tests/ -q` grün, alle P0 abgeschlossen.
- [ ] **Sprint 2:** `pytest tests/ -q` grün, alle P1-Findings der Cluster E/F/G/I abgeschlossen.
- [ ] **Sprint 3:** `pytest tests/ -q` grün, Coverage ≥ 55 %, alle P1 abgeschlossen.
- [ ] **Sprint 4 (optional):** Coverage ≥ 60 %, Mutation-Score ≥ 70 %, alle P2 abgeschlossen.

---

## 📎 Anhang

- **Bezugs-Review:** `docs/reports/2026-06-14_code-review.md`
- **P1-Cluster (Phase 2):** `2026-06-14_m4_findings_phase2_p1.md`
- **P2-Cluster (Phase 3):** `2026-06-14_m4_findings_phase3_p2.md`
- **Sub-Plan-Übersicht:** `2026-06-14_m4_anhang_b_subplans.md`
- **Vorgänger-Pläne:**
  - `plans/2026-06-12_m1_main_window_refactor.md` (M-1, abgeschlossen)
  - `plans/2026-06-12_m2_choraufstellung_refactor.md` (M-2, abgeschlossen)
  - `plans/2026-06-13_m3_dialogs_refactor.md` (M-3, abgeschlossen)

**Definitions:**
- **XS** = < 0,1 Personentag
- **S** = 0,1–0,5 Personentage
- **M** = 0,5–2 Personentage
- **L** = 2–5 Personentage
- **XL** = > 5 Personentage (immer Sub-Plan erforderlich)

**ID-Schema:**
- `<Finding>-FIX-<Letter>` — z. B. `C2-FIX-A`
- `<Finding>-SUBPLAN-<Letter>` — Sub-Plan, z. B. `C3-SUBPLAN-A`
- `.N` — Sub-Task innerhalb einer Aufgabe

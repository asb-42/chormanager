"""Forms package: extracted complex input dialogs from main_window.py.

M-1 step 1: SingerDialog was the first form moved out of the
God-Class. The package name was deliberately chosen as ``forms``
(not ``dialogs``) to avoid name collisions with the existing
``chormanager/ui/dialogs.py`` module.
"""
